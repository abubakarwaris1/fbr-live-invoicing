name: Deploy Application

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests (if any)
        run: npm test --if-present

      - name: Build client
        run: npm run build:client

  deploy:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    environment: production
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH key
        run: |
          # Create SSH directory with proper permissions
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          
          # Write SSH key to file (handle potential formatting issues)
          echo "${{ env.SERVER_SSH_KEY }}" | tr -d '\r' > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          
          # Validate SSH key format
          if ! ssh-keygen -l -f ~/.ssh/id_rsa > /dev/null 2>&1; then
            echo "❌ Invalid SSH key format"
            exit 1
          fi
          
          # Add server to known_hosts
          ssh-keyscan -H ${{ env.STATIC_IP }} >> ~/.ssh/known_hosts
          chmod 644 ~/.ssh/known_hosts
          
          # Test SSH connection with retry
          for i in {1..3}; do
            if ssh -o ConnectTimeout=10 -o StrictHostKeyChecking=no root@${{ env.STATIC_IP }} "echo 'SSH connection successful'"; then
              echo "✅ SSH connection successful"
              break
            else
              echo "❌ SSH connection failed, attempt $i/3"
              if [ $i -eq 3 ]; then
                echo "❌ SSH connection failed after 3 attempts"
                exit 1
              fi
              sleep 5
            fi
          done

      - name: Deploy application to server
        run: |
          # Create tmp directory if it doesn't exist
          mkdir -p /tmp
          
          # Create tar file
          tar -czf /tmp/fbr-live-invoicing.tar.gz \
            --exclude='.git' \
            --exclude='node_modules' \
            --exclude='.env' \
            --exclude='terraform/.terraform' \
            --exclude='terraform/terraform.tfstate*' \
            .
          
          # Copy to server
          scp -o ConnectTimeout=30 -o StrictHostKeyChecking=no \
            /tmp/fbr-live-invoicing.tar.gz \
            root@${{ env.STATIC_IP }}:/tmp/
          
          # Deploy on server
          ssh -o ConnectTimeout=10 -o StrictHostKeyChecking=no root@${{ env.STATIC_IP }} << 'REMOTE_SCRIPT'
            set -e
            echo "🚀 Starting deployment with nginx inside container..."
            
            # Create application directory
            mkdir -p /opt/fbr-live-invoicing
            cd /opt/fbr-live-invoicing
            
            # Stop and remove existing containers
            echo "🛑 Stopping and removing existing containers..."
            docker-compose down || true
            docker stop fbr-live-invoicing || true
            docker rm -f fbr-live-invoicing || true
            
            # Remove old images to save space
            echo "🧹 Cleaning up old images..."
            docker rmi fbr-live-invoicing:latest || true
            docker system prune -f || true
            
            # Extract application files
            echo "📁 Extracting application files..."
            rm -rf /opt/fbr-live-invoicing/*
            tar -xzf /tmp/fbr-live-invoicing.tar.gz -C /opt/fbr-live-invoicing/
            rm -f /tmp/fbr-live-invoicing.tar.gz
            
            # Create .env file with environment variables (FIXED: Using env.* syntax)
            echo "📝 Creating environment file..."
            cat > /opt/fbr-live-invoicing/.env << 'EOL'
            NODE_ENV=production
            PORT=3001
            MONGODB_URI=${{ env.MONGODB_URI }}
            JWT_SECRET=${{ env.JWT_SECRET }}
            FBR_ACCESS_TOKEN=${{ env.FBR_ACCESS_TOKEN }}
            STATIC_IP=${{ env.STATIC_IP }}
            EOL
            
            # Ensure Docker is running
            echo "🐳 Ensuring Docker is running..."
            systemctl start docker || true
            systemctl enable docker || true
            
            # Wait for Docker to be ready
            echo "⏳ Waiting for Docker to be ready..."
            for i in {1..10}; do
              if docker info > /dev/null 2>&1; then
                echo "✅ Docker is ready"
                break
              else
                echo "⏳ Docker not ready, attempt $i/10"
                sleep 3
              fi
            done
            
            # Build and start the container with nginx inside
            echo "🔨 Building and starting container with nginx inside..."
            cd /opt/fbr-live-invoicing
            docker-compose up -d --build
            
            # Wait for application to start with better detection
            echo "⏳ Waiting for application to start..."
            for i in {1..20}; do
              if docker-compose ps | grep -q "Up"; then
                echo "✅ Container is up"
                break
              else
                echo "⏳ Container not up yet, attempt $i/20"
                sleep 3
              fi
            done
            
            # Additional wait for services to be ready
            sleep 10
            
            # Test health endpoint (nginx health check inside container)
            echo "🔍 Testing nginx health endpoint inside container..."
            for i in {1..10}; do
              if curl -f http://localhost:3000/health; then
                echo "✅ Nginx health check passed!"
                break
              else
                echo "❌ Nginx health check failed, attempt $i/10"
                sleep 5
              fi
            done
            
            # Test API endpoint through container nginx
            echo "🔍 Testing API endpoint through container nginx..."
            for i in {1..10}; do
              if curl -f http://localhost:3000/api/health; then
                echo "✅ API health check through container nginx passed!"
                break
              else
                echo "❌ API health check through container nginx failed, attempt $i/10"
                sleep 5
              fi
            done
            
            # Test frontend through container nginx
            echo "🔍 Testing frontend through container nginx..."
            for i in {1..10}; do
              if curl -f http://localhost:3000/ | grep -q "html"; then
                echo "✅ Frontend check through container nginx passed!"
                break
              else
                echo "❌ Frontend check through container nginx failed, attempt $i/10"
                sleep 5
              fi
            done
            
            # Show container status
            echo "📊 Container status:"
            docker-compose ps
            
            # Show logs
            echo "📋 Recent logs:"
            docker-compose logs --tail=20
            
            echo "🎉 Deployment completed successfully!"
          REMOTE_SCRIPT

      - name: Comment deployment info
        uses: actions/github-script@v7
        if: github.event_name == 'pull_request'
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `🚀 **Application Deployment Successful!**
              
              **Static IP:** ${{ env.STATIC_IP }}
              **Web Interface:** http://${{ env.STATIC_IP }}:3000/
              **API Base URL:** http://${{ env.STATIC_IP }}:3000/api
              **Health Check:** http://${{ env.STATIC_IP }}:3000/health
              **API Health:** http://${{ env.STATIC_IP }}:3000/api/health
              
              **Architecture:** Container nginx on port 3000 → Express.js on port 3001`
            })

  notify:
    needs: [deploy]
    runs-on: ubuntu-latest
    if: always()
    
    steps:
      - name: Notify deployment status
        run: |
          if [ "${{ needs.deploy.result }}" == "success" ]; then
            echo "✅ Application deployment successful!"
            echo "🌐 Web Interface: http://${{ env.STATIC_IP }}:3000/"
            echo "🔗 API Base URL: http://${{ env.STATIC_IP }}:3000/api"
          else
            echo "❌ Application deployment failed!"
          fi
